# Azure Authentication Environment Variables
# Copy this file to .env and fill in your values
# SECURITY: Never commit .env files with real values to git

# Required: Azure Subscription ID for Terraform authentication
# Get this from: az account show --query id --output tsv
ARM_SUBSCRIPTION_ID=your-subscription-id-here

# Optional: Azure Tenant ID (usually auto-detected)
# ARM_TENANT_ID=your-tenant-id-here

# Optional: Azure Client ID (for service principal authentication)
# ARM_CLIENT_ID=your-client-id-here

# Optional: Azure Client Secret (for service principal authentication)
# ARM_CLIENT_SECRET=your-client-secret-here

# Terraform Variables (provide these via environment)
# TF_VAR_session_secret=your-session-secret-here
# TF_VAR_ssl_certificate_password=your-cert-password-here
# TF_VAR_core_function_package_url=https://your-storage.blob.core.windows.net/container/package.zip

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your values in .env (this file is gitignored)
# 3. Source the file: source .env
# 4. Or export manually: export ARM_SUBSCRIPTION_ID="your-subscription-id"

# Generate secure secrets:
# Session secret: openssl rand -base64 32
# Certificate password: openssl rand -base64 16
